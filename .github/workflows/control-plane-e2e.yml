name: Control Plane E2E

on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/control-plane-e2e.yml'

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Runner smoke
        run: echo control-plane-e2e-runner-ok
